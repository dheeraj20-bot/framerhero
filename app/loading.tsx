export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        <div className=" h-screen flex flex-col bg-black-100 items-center justify-center">Loading</div>
    )
  }